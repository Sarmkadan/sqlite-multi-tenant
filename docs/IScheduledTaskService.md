# IScheduledTaskService

The `IScheduledTaskService` interface and its related types provide a mechanism for registering, managing, and executing scheduled background tasks in a multi-tenant SQLite environment. Tasks can be configured with intervals, enabled/disabled states, and tracked for execution status, errors, and timing.

## API

### `ScheduledTaskService` (implementation of `IScheduledTaskService`)

#### Constructor
