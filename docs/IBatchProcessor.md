# IBatchProcessor

`IBatchProcessor` defines an interface for processing collections of items in batches, collecting results and errors, and reporting outcomes. It is implemented by `BatchProcessor`, a sealed class that provides asynchronous batch processing with configurable concurrency and error handling.

## API

### `BatchProcessor` (sealed class)

#### Constructor
