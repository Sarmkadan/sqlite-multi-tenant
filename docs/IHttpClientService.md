# IHttpClientService

A lightweight HTTP client abstraction for making typed HTTP requests with configurable retry, timeout, and connection pooling policies. Designed for multi-tenant applications where isolation and resilience are required.

## API

### `HttpClientService` (class)

#### Constructor
